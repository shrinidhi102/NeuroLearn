<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>NeuroLearn</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@500;700&display=swap" rel="stylesheet">
  <style>
    /* Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(to right, #f5f9ff, #f5f9ff);
      color: #222;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      font-size: 1.5rem;
    }

    /* Header */
    header {
      background: #0077cc;
      color: white;
      padding: 40px 20px;
      text-align: center;
      border-bottom: 5px solid #005fa3;
    }

    .top-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      max-width: 1100px;
      margin: 0 auto;
      flex-wrap: wrap;
    }

    h1 {
      font-size: 2.8rem;
    }

    .subtitle {
      font-size: 1.8rem;
      margin-top: 10px;
    }

    .profile-box {
      font-size: 1.2rem;
      text-align: right;
    }

    .profile-box strong {
      margin-right: 10px;
    }

    .logout-button {
      font-size: 1rem;
      background: none;
      color: white;
      border: 1px solid white;
      padding: 5px 10px;
      border-radius: 5px;
      cursor: pointer;
      margin-left: 10px;
    }

    .login-button {
      font-size: 1.2rem;
      background: white;
      color: #0077cc;
      padding: 8px 20px;
      border-radius: 10px;
      text-decoration: none;
      font-weight: bold;
      border: none;
      transition: background 0.3s ease;
    }

    .login-button:hover {
      background: #005fa3;
      color: white;
    }

    /* Main Buttons */
    .choice-container {
      display: flex;
      justify-content: center;
      gap: 50px;
      padding: 80px 20px;
      flex-wrap: wrap;
    }

    .option-button {
      font-size: 1.8rem;
      padding: 30px 60px;
      background-color: white;
      color: #0077cc;
      border: 4px solid #0077cc;
      border-radius: 20px;
      cursor: pointer;
      font-weight: bold;
      transition: all 0.3s ease;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    .option-button:hover {
      background-color: #0077cc;
      color: white;
      transform: scale(1.05);
    }

    /* Footer */
    footer {
      text-align: center;
      padding: 30px;
      background-color: #f2f2f2;
      font-size: 1.3rem;
      color: #444;
      margin-top: auto;
    }
  </style>
</head>
<body>
  <header>
    <div class="top-bar">
      <h1>NeuroLearn</h1>
      <div id="user-profile" class="profile-box"></div>
    </div>
    <p class="subtitle">Select your area of focus</p>
  </header>

  <main>
    <div class="choice-container">
      <button class="option-button" onclick="window.location.href='dyslexia.html'">📘 Dyslexia Support</button>
      <button class="option-button" onclick="window.location.href='adhd.html'">⚡ ADHD Support</button>
    </div>
  </main>

  <footer>
    <p>Helping you learn in your own way. 💙</p>
  </footer>

  <script>
    const username = localStorage.getItem("neuro_user");
    const profileBox = document.getElementById("user-profile");

    if (username) {
      profileBox.innerHTML = `
        👤 <strong>${username}</strong>
        <button class="logout-button" onclick="logout()">Logout</button>
      `;
    } else {
      profileBox.innerHTML = `<a href="login.html" class="login-button">Login</a>`;
    }

    function logout() {
      localStorage.removeItem("neuro_user");
      location.reload();
    }
  </script>
</body>
</html>
